\documentclass{standalone}
\usepackage{tikz}
\usepackage{fontspec}
\usepackage{xeCJK}

% 设置中文字体为宋体五号字
\setCJKmainfont{SimSun}
% 设置英文字体为五号Times New Roman
\setmainfont{Times New Roman}
\begin{document}
	
	\begin{tikzpicture}[node distance=3cm, every node/.style={align=center}]
		
		% Nodes
		\node (app) {应用层 \\ Application};
		\node [below of=app] (mqtt) {MQTT};
		\node [below of=mqtt] (tcp) {传输层 \\ TCP};
		\node [below of=tcp] (ip) {网络层 \\ IP};
		\node [below of=ip] (phys) {物理层 \\ Physical};
		
		% Arrows
		\draw[->] (app) -- (mqtt);
		\draw[->] (mqtt) -- (tcp);
		\draw[->] (tcp) -- (ip);
		\draw[->] (ip) -- (phys);
		
		% Labels
		\node [above left=0.5cm] at (mqtt.north west) {发布/订阅 \\ Publish/Subscribe};
		\node [above left=0.5cm] at (tcp.north west) {传输层 \\ Transport};
		\node [above left=0.5cm] at (ip.north west) {网络层 \\ Internet};
		\node [above left=0.5cm] at (phys.north west) {物理层 \\ Physical};
		
	\end{tikzpicture}
	
\end{document}

